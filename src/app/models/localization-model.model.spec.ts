import { LocalizationModel } from './localization-model.model';

describe('LocalizationModel', () => {
  it('should create an instance', () => {
    expect(new LocalizationModel()).toBeTruthy();
  });
});
