import { APIIntegrationModel } from './apiintegration-model.model';

describe('APIIntegrationModel', () => {
  it('should create an instance', () => {
    expect(new APIIntegrationModel()).toBeTruthy();
  });
});
