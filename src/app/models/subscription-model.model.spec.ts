import { SubscriptionModel } from './subscription-model.model';

describe('SubscriptionModel', () => {
  it('should create an instance', () => {
    expect(new SubscriptionModel()).toBeTruthy();
  });
});
