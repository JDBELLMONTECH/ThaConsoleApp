import { NotificationsData } from './notifications-data.data';

describe('NotificationsData', () => {
  it('should create an instance', () => {
    expect(new NotificationsData()).toBeTruthy();
  });
});
